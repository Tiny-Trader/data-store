[Unit]
Description=Run tiny-trader EOD candle sync daily

[Timer]
# Weekdays after market close (IST). Persistent runs a missed job after downtime.
OnCalendar=Mon-Fri 18:30 Asia/Kolkata
Persistent=true

[Install]
WantedBy=timers.target
