# Deploy

**Hosting / production deploy is intentionally out of scope for this public
repo.** Run the service locally (see the root `README.md`) or host it however
you like.

Maintainer-only notes, host inventory, keys, and backup config live under
`deploy/private/` on the author’s machine (gitignored) — not published.
